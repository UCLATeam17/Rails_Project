<p id="notice"><%= notice %></p>

<table>
<tr>
  <h2>
    <%= @category.name %>
  </h2>
</tr>
<% @category.courses.each do |course| %>
<tr>
  <td>
    <%= link_to course.name, course %>
  </td>
</tr>
<% end %>
</table>

