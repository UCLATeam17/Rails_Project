<%= form_for(@course) do |f| %>
  <% if @course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% @course.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div><%= f.label :category_id %><br />
  <%= f.collection_select(:category_id, Category.all, :_id, :name, :prompt=>"Please Select")%><%= link_to 'New Category', new_category_path%></div>

  <div><%= f.label :name %><br />
  <%= f.text_field :name %> </div>

  <div><%= f.label :description %><br />
  <%= f.text_area :description, :size => "30x15" %> </div>

  <div><%= f.label :price %> €<br/>
  <div><%= f.text_field :price %></div>
  
  <div><%=f.label :start_date %><br/>
  <%= f.date_select :start_date, :order => [:day, :month, :year] %></div>

  <div><%=f.label :end_date %><br/>
  <%= f.date_select :end_date, :order => [:day, :month, :year] %></div>

  <div><%= f.label :location %><br />
  <%= f.text_field :location %></div>

  <div><%= f.label :num_places %><br />
  <%= f.text_field :num_places %></div>

  <div><%= f.label :tags %><br />
  <%= f.text_field :tags%></div>

  <br />

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
